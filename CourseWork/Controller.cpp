#include "Controller.h"

/*
template <class CType> Controller<CType>::Controller()
{
}

template <class CType> void Controller<CType>::add(CType node)
{
	_container.push_back( node );
}

template <class CType> void Controller<CType>::del(int id)
{
	if (validateID(id))
		_container.erase(_container.begin() + id);
}

template <class CType> void Controller<CType>::del(CType node)
{
    for (typename vector<CType>::iterator iter = _container.begin(); iter != _container.end(); iter++)
		if (*iter == node)
			_container.erase(iter);
}

template <class CType> vector<CType> Controller<CType>::getContainer()
{
	return _container;
}

template <class CType> bool Controller<CType>::validateID(int id)
{
	return (id >= 0) && (id < _container.size());
}
*/
